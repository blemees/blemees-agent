{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://blemees/schemas/outbound/session.attention_cleared.json",
  "title": "session.attention_cleared",
  "description": "Emitted when a session leaves needs_attention — an owner re-attached or the cause resolved (#24, §6).",
  "type": "object",
  "properties": {
    "type": { "const": "session.attention_cleared" },
    "session_id": { "$ref": "https://blemees/schemas/_common.json#/$defs/SessionId" },
    "seq": { "$ref": "https://blemees/schemas/_common.json#/$defs/Seq" }
  },
  "required": ["type"],
  "additionalProperties": true
}
