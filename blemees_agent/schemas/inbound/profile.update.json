{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://blemees/schemas/inbound/profile.update.json",
  "title": "profile.update",
  "description": "Replace a dynamic profile's definition, persisted to the registry (#25). Config-managed profiles are rejected (profile_protected); a missing binary is agent_unavailable. Replied with profile.updated.",
  "type": "object",
  "properties": {
    "type": { "const": "profile.update" },
    "id": { "$ref": "https://blemees/schemas/_common.json#/$defs/RequestId" },
    "name": { "$ref": "https://blemees/schemas/_common.json#/$defs/ProfileName" },
    "profile": { "$ref": "https://blemees/schemas/_common.json#/$defs/ProfileSpec" }
  },
  "required": ["type", "profile"],
  "additionalProperties": false
}
